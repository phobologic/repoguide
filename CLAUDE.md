# repoguide

@import ../../claude_code/go/CLAUDE.md

## Commands

```
go build -o repoguide .     # Build binary
go run .                     # Run without building
go test ./...                # Run all tests
goimports -w .               # Format and fix imports
golangci-lint run ./...      # Lint
```
